#!/bin/bash

if [ "$1" == "post-processing" ]; then
  cd post-processing
  pipenv run script $2 $3 $4 $5 $6 $7
elif [ "$1" == "download-boundary-data" ]; then
  cd download-boundary-data
  pipenv run script
else 
  echo "Unknown script specified: $1"
fi